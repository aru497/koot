# local preview/test renders (not part of the site)
roo*.png
aus*.png
*_prev.png

# local SEO audit docs (not served)
FULL-AUDIT-REPORT.md
ACTION-PLAN.md

# OS / editor / tooling artifacts
.DS_Store
.fuse_hidden*
supabase/.temp/

# local secrets — NEVER committed (repo is public)
*.local.md
.env
.env.*
